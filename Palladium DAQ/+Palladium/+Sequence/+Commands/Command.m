classdef Command < handle
    %COMMAND 

    %% Properties (Public)
    properties(Access = public)
        
    end

    %% Constructor
    methods
        function this = Command()
        end
    end

    %% Methods (Public)
    methods(Access = public)

    end
end